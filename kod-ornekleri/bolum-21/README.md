# Bölüm 21 — VECTOR Tipi ve DiskANN

VECTOR(N) tipi, VECTOR_DISTANCE, CREATE VECTOR INDEX (DiskANN).

| Dosya | Amaç |
|---|---|
| `01-vector-tipi-temel.sql` | VECTOR sütun, insert, JSON'dan parse, dimension karşılaştırma |
| `02-vector-distance-fonksiyonlari.sql` | COSINE, EUCLIDEAN, DOT — manuel hesap vs fonksiyon |
| `03-diskann-index.sql` | CREATE VECTOR INDEX, parametreler (Vamana R, alpha, FP16) |
| `04-vector-property.sql` | VECTORPROPERTY ile boyut, format, metric metadata |
